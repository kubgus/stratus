:root {
    --bg-color: #1c1c1c;
    --secondary-bg-color: #242424;
    --text-color: #ffffff;
    --primary-color: #10ea72;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    background-color: #1c1c1c;
    color: var(--text-color);
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    padding: 1rem;
}

h1 {
    color: var(--primary-color);
}

input, button {
    padding: 0.5rem;
    border-radius: 0.5rem;
    border: none;
    background-color: var(--secondary-bg-color);
    color: var(--text-color);
    outline: none;
    cursor: pointer;
    transition: 0.2s all;
}

button:hover, input[type="submit"]:hover {
    background-color: var(--primary-color);
}

#logout {
    background-color: var(--primary-color);
}

form {
    display: flex;
    flex-direction: row;
    gap: 0.22rem;
    margin: 1rem 0;
}

form.logreg {
    flex-direction: column;
    gap: 0.5rem;
    padding: 0 30vw;
}

#subdomains-container {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    background-color: var(--secondary-bg-color);
    padding: 1rem;
    border-radius: 0.5rem;
}

#subdomains-container h2 {
    margin-bottom: 0.5rem;
}

#subdomains-container > section {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}

#subdomains-container > section > div > div {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    gap: 0.5rem;
    background-color: var(--bg-color);
    padding: 0.5rem;
    border-radius: 0.5rem;
}

.token-container {
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 0.5rem;
}
